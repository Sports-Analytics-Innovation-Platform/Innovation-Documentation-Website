# AI Usage Ledger

| Date | Author | Tool | Model | Purpose | Files/Docs Affected | Link | Transcript |
|---|---|---|---|---|---|---|---|
| 2026-08-06 | Josh Sawyer | Claude Code | Claude Sonnet 5    | Initial project scaffold: monorepo structure, Express+Prisma API (auth, players/teams/stats endpoints), mock NBA seed data, React+Vite+Tailwind frontend (player list + profile pages), Docker Compose for local Postgres, root README | `apps/api/**`, `apps/web/**`, `docker-compose.yml`, `README.md`, `.gitignore` | | [JoshSawyer-conversation-2026-08-12-204630.txt](transcripts/ai_transcripts/JoshSawyer-conversation-2026-08-12-204630.txt) |
| 2026-08-06 | Owen | Claude Code | Claude Sonnet 5    | Migrated the API backend from Express to NestJS (modules/controllers/guards/global exception filter), preserving existing auth, players, and teams behaviour exactly; step 1 of moving the stack to the agreed tech choices | `apps/api/**`, `README.md` | not recoverable | [Owen Claude Auth](transcripts/ai_transcripts/2026-08-08-Owen-Claude-Auth.txt)
| 2026-08-07 | Owen | Claude Code | Claude Sonnet 5 | Added test suites for both apps (Vitest unit tests + Supertest e2e tests against a real Postgres instance for the API; Vitest + React Testing Library for the frontend), a GitLab CI pipeline running both with coverage on a Postgres service container, and a script merging both apps' coverage into one HTML dashboard published to GitLab Pages and posted as an MR comment | `apps/api/src/**/*.spec.ts`, `apps/api/test/**`, `apps/api/vitest.config.ts`, `apps/api/.env.test.example`, `apps/web/src/**/*.spec.{ts,tsx}`, `apps/web/src/test/**`, `apps/web/vite.config.ts`, `scripts/**`, `.gitlab-ci.yml`, `docker-compose.yml`, `README.md` | | Not yet provided |
| 2026-08-12 | Owen | Claude Code | Claude Sonnet 5 | Added `GIT_METHODOLOGY.md`, `PROJECT_METHODOLOGY.md`, and `PROJECT_OVERVIEW.md` (git conventions, Scrum-adapted-to-sprints methodology, and a living technical reference) | `docs/GIT_METHODOLOGY.md`, `docs/PROJECT_METHODOLOGY.md`, `docs/PROJECT_OVERVIEW.md` | | Not yet provided |
| 2026-08-13 | Owen | Claude Code | Claude Sonnet 5 | Added a `GET /v1/games` endpoint, replaced the sidebar with a top navbar (with an honest "recent result" widget), redesigned the home page hero, and fixed three real bugs found in the existing test suite (e2e specs weren't matching the vitest include glob, were flaky from parallel files racing one shared database, and the test app's Express version didn't match production's) | `apps/api/src/games/**`, `apps/api/test/**`, `apps/api/vitest.config.ts`, `apps/web/src/components/Navbar.tsx`, `apps/web/src/pages/HomePage.tsx` | | |
| 2026-08-15 | Owen | Claude Code | Claude Sonnet 5 | API and frontend work: additional API endpoint development and frontend integration | `apps/api/src/**`, `apps/web/src/**` | | [2026-08-15-Owen-Claude-API-FrontEnd.txt](transcripts/ai_transcripts/2026-08-15-Owen-Claude-API-FrontEnd.txt) |
| 2026-08-13 | Owen | Claude Code | Claude Sonnet 5 | Built a fantasy-lineup optimizer: a Python service (`apps/optimizer`) predicting per-player fantasy points and solving a 5-player lineup under a salary cap via MILP (PuLP/CBC), new Prisma tables, a `GET /v1/optimizer/lineup` endpoint, and a frontend Optimizer page | `apps/optimizer/**`, `apps/api/src/optimizer/**`, `apps/api/prisma/schema.prisma`, `apps/web/src/pages/OptimizerPage.tsx` | | |
| 2026-08-14 | Owen | Claude Code | Claude Sonnet 5 | Merged this ledger with the previously-separate `docs/AI_USAGE.md`, removing the duplicate | `docs/ai-usage.md` | | |
| 2026-08-14 | Adrian | Claude.ai | Claude Sonnet 5 | Scaffolded MkDocs site, debugged GitHub Pages deploy, drafted meeting-doc structure | mkdocs.yml, docs/meetings/, docs/ai-usage.md | [link](https://claude.ai/share/598eee84-8e5d-41eb-9677-f31ea341246f) |[2026-08-14-adrian-claude-doc-website.txt](transcripts/ai_transcripts/2026-08-14-adrian-claude-doc-website.txt) |
| 2026-08-14 | Adrian | ChatGPT | GPT-5.6 Luna | Generated MkDocs meeting minutes from a client/standup transcript, refined the meeting template and LLM summarisation prompt, and added collapsible meeting formatting | mkdocs.yml, docs/ai-usage.md, docs/meetings/_template.md, docs/meetings/client/index.md, docs/meetings/how-to-summarize.md, docs/meetings/index.md, docs/meetings/standups/index.md, docs/transcripts/2026-08-12-standup.txt, docs/transcripts/2026-08-13-client.txt | [link](https://chatgpt.com/share/6a8018bb-a464-83ea-a664-a6ecb475ff47) | [2026-08-14-adrian-chatgpt-meeting-summaries.txt](transcripts/ai_transcripts/2026-08-14-adrian-chatgpt-meeting-summaries.txt) |
| 2026-08-15 | Adrian | Claude.ai | Claude Sonnet 5 | Drafted MkDocs documentation site content across process, tech-stack, design, and decision-record pages; corrected mkdocs.yml site name and git-host terminology; drafted per-file commit messages; compiled team-questions and handoff docs for the next session | mkdocs.yml, docs/coding-conventions.md, docs/git-methodology.md, docs/methodology.md, docs/definition-of-done.md, docs/requirements.md, docs/tech-stack.md, docs/security.md, docs/getting-started.md, docs/design/architecture.md, docs/design/erd.md, docs/design/api-design.md, docs/design/wireframes.md, docs/decisions/adr-001-database.md, docs/decisions/adr-002-auth.md, team-questions.md, claude-handoff.md | https://claude.ai/share/26395a12-8801-40f7-b309-c459f06393a5 | [First draft of docs](transcripts/ai_transcripts/2026-08-15-adrian-claude-docs-first-draft.txt) |
| 2026-08-16 | Adrian | Claude Web | Claude Sonnet 5 | Iteration 2 of docs-site work: reconciled the tech stack against real code (BetterAuth replacing Passport.js, confirmed schema/API/ERD from source), rewrote ADR-002 and flagged an unresolved password-reset compliance risk, unblocked feature-tiers.md and roadmap.md once the team defined what "optimisation" means, added a rubric-links.md quick-reference page, fixed a broken-links CI failure (meetings/standups → meetings/scrum rename), and produced an updated handoff doc + team-questions.md for the next iteration | docs/tech-stack.md, docs/decisions/adr-002-auth.md, docs/design/erd.md, docs/design/architecture.md, docs/security.md, docs/getting-started.md, docs/requirements.md, docs/design/feature-tiers.md, docs/design/roadmap.md, docs/design/api-design.md, docs/design/wireframes.md, docs/rubric-links.md, docs/index.md, mkdocs.yml, docs/meetings/index.md, team-questions.md, claude-handoff.md | https://claude.ai/share/15b57d21-0705-4cc0-bb16-a770e078d275 | [second iteration of docs](transcripts/ai_transcripts/2026-08-16-adrian-claude-docs-second-draft.txt) |
| 2026-08-17 | Owen | Claude Code | Claude Sonnet 5 | Synced local project with remote, ran full project, fixed CI issues, documentation sync, and various code changes across the API and frontend | `apps/api/**`, `apps/web/**`, `docs/**` | | [2026-08-17-Owen-Claude-Sync-Documentation.txt](transcripts/ai_transcripts/2026-08-17-Owen-Claude-Sync-Documentation.txt) |
| 2026-08-18 | Josh Sawyer | Claude Code | Claude Sonnet 5 | Reviewed and merged the `sync-main-to-lineup-optimizer` branch into `main` (113 files) after confirming it was the up-to-date branch, not the reverse | `apps/**` (merge) | | [JoshSawyer-conversation-2026-08-18-transcript.txt](transcripts/ai_transcripts/JoshSawyer-conversation-2026-08-18-transcript.txt) |
| 2026-08-18 | Josh Sawyer | Claude Code | Claude Sonnet 5 | Researched sports-analytics literature (Elo ratings, Dean Oliver's Four Factors) and built `apps/predictor` (win-probability + score-margin prediction), a `GamePrediction` model, `GET /v1/games/:id/prediction`, a Predictions page, a game-detail page with per-player scoring predictions, and an illustrative SVG court view — plus fixed a real N+1 query bug and a navbar layout bug found along the way | `apps/predictor/**`, `apps/api/src/predictions/**`, `apps/api/src/games/**`, `apps/api/prisma/schema.prisma`, `apps/web/src/pages/PredictionsPage.tsx`, `apps/web/src/pages/GameDetailPage.tsx`, `apps/web/src/components/CourtView.tsx`, `apps/web/src/components/Navbar.tsx` | | [JoshSawyer-conversation-2026-08-18-transcript.txt](transcripts/ai_transcripts/JoshSawyer-conversation-2026-08-18-transcript.txt) |
| 2026-08-18 | Josh Sawyer | Claude Code | Claude Sonnet 5 | Researched the `nba_api` package (rate limits, cloud IP blocking, a deprecated boxscore endpoint) via live testing and built `apps/ingestion`, a real NBA data pipeline (30 teams, 530 players, 243 games, 6,051 boxscore rows from stats.nba.com), replacing the 4-team mock seed data | `apps/ingestion/**` | | [JoshSawyer-conversation-2026-08-18-transcript.txt](transcripts/ai_transcripts/JoshSawyer-conversation-2026-08-18-transcript.txt) |
| 2026-08-18 | Josh Sawyer | Claude Code | Claude Sonnet 5 | Migrated the database from local Docker Postgres to a hosted Supabase instance (ran existing Prisma migrations, copied ingested data over, split connections by pooler type) | `apps/api/.env`, `apps/ingestion/.env` (local, not committed) | | [JoshSawyer-conversation-2026-08-18-transcript.txt](transcripts/ai_transcripts/JoshSawyer-conversation-2026-08-18-transcript.txt) |
| 2026-08-18 | Josh Sawyer | Claude Code | Claude Sonnet 5 | Added real team logos and player headshots via nba.com's CDN, computed from existing `nbaTeamId`/`nbaPlayerId` fields with a graceful fallback to the existing colored-initial badges | `apps/web/src/components/TeamBadge.tsx`, `apps/web/src/components/PlayerHeadshot.tsx` | | |
| 2026-08-19–20 | Adrian | Qoder | Gwen3.7-Plus, Qoder Lite, Qoder Auto, Kimi-K2.7-Code | Diagnosed and resolved Render deployment failures (TypeScript build errors from missing `@types/node`, devDependencies skipped by `npm ci` under `NODE_ENV=production`, Prisma CLI version mismatch), configured Render Blueprint (`render.yaml`) for free-tier NestJS API, ran Prisma migrations against Supabase, set up Gitea push mirror to GitHub for auto-deploy, and documented hosting topology (Cloudflare Pages + Render + Supabase) in ADR-003 | `render.yaml`, `apps/api/tsconfig.json`, `docs/decisions/ADR-003-hosting-topology.md`, `apps/web/src/lib/apiClient.ts`, `apps/web/src/lib/authClient.ts`, `apps/web/public/_redirects`, `apps/web/.env.example`, `apps/web/src/vite-env.d.ts` | not recoverable| [2026-08-19-adrian-qoder-render-deploy.jsonl](transcripts/ai_transcripts/2026-08-19-adrian-qoder-render-deploy.jsonl) |
| 2026-08-20 | Adrian | Qoder | Gwen3.7-Plus, Qoder Lite | Installed project skills (code-cleanup, coding-conventions, team-git-workflow), removed redundant mirror workflow, built frontend for Cloudflare Pages deployment, connected Cloudflare to GitHub mirror for auto-deploy, resolved GitHub secret scanning block via git filter-repo history rewrite, restored GitHub Desktop push routing to Gitea, and updated Render deploy branch from hosting-topology to main | `.qoder/skills/**`, `.gitea/workflows/mirror.yml`, `render.yaml` | not recoverable | [2026-08-20-adrian-qoder-cloudflare-deploy.jsonl](transcripts/ai_transcripts/2026-08-20-adrian-qoder-cloudflare-deploy.jsonl)|
| 2026-08-20 | Josh Sawyer | Claude Code | Claude Sonnet 5 | Ingestion service development and predictor work | `apps/ingestion/**`, `apps/predictor/**` | | [JoshSawyer-conversation-2026-08-20-transcript.txt](transcripts/ai_transcripts/JoshSawyer-conversation-2026-08-20-transcript.txt) |
| 2026-08-21 | Adrian | Qoder | Qoder Lite | Diagnosed and resolved MkDocs strict-mode build failures (14 warnings aborting GitHub Pages deploy): fixed broken transcript links in `ai-usage.md` (wrong folder name `ai-transcripts` → `ai_transcripts`, missing `.txt` extension, non-existent `OwenPace_01.txt`), converted 11 out-of-repo links in ADR-003 to plain code references, renamed `ADR-003-hosting-topology.md` to lowercase to match nav, and added `_template.md` / `how-to-summarize.md` to the Meetings nav | `docs/ai-usage.md`, `docs/decisions/adr-003-hosting-topology.md`, `mkdocs.yml` | not recoverable | [2026-08-21-adrian-qoder-fix-broken-links.jsonl](transcripts/ai_transcripts/2026-08-21-adrian-qoder-fix-broken-links.jsonl) | 
| 2026-08-21 | Daniel Passos | OpenAI Codex | GPT-5.6 Sol | Added code coverage reporting to Gitea Actions, including API and Web coverage runs, a combined HTML coverage dashboard, downloadable CI artifact, and related documentation updates; reviewed and tested the workflow on the Gitea runner | `.gitea/workflows/ci.yml`, `README.md`, `docs/PROJECT_OVERVIEW.md` | | [Coverage reporting](transcripts/ai_transcripts/DanielPassos_01_CoverageReporting.txt) |
| 2026-08-21 | Daniel Passos | OpenAI Codex | GPT-5.6 Sol | Implemented server-side player and team search across the full dataset with pagination, existing filters, frontend debounce and API/frontend tests; also added authenticated route and API protection for predictions, games and optimizer while keeping player and team pages public | `apps/api/src/players/**`, `apps/api/src/teams/**`, `apps/api/src/games/**`, `apps/api/src/optimizer/**`, `apps/web/src/pages/PlayersListPage.tsx`, `apps/web/src/pages/TeamsListPage.tsx`, `apps/web/src/components/PlayersFilterBar.tsx`, `apps/web/src/components/ProtectedRoute.tsx`, `apps/web/src/components/AuthStatus.tsx`, `apps/web/src/components/RecentResultWidget.tsx`, `apps/web/src/lib/nbaApi.ts`, `apps/web/src/lib/authClient.ts`, `apps/web/src/App.tsx` | | [Search and access-control work](transcripts/ai_transcripts/DanielPassos_02_Actions.txt) |
| 2026-08-23 | Adrian | Qoder | Qoder | Updated methodology to dynamic/initiative-based Scrum (replaced sprint review with sprint reflection, removed pre-assigned tasks in favour of self-selection from product backlog); added `sprint-log.md` with full Sprint 1 week-by-week task tables to docs and nav; performed full audit of all 15 doc pages identifying every draft marker, TBD, placeholder, and outdated "not yet built" warning; produced a handoff document cataloguing all fixes needed for the final docs polish pass | `docs/methodology.md`, `docs/sprint-log.md`, `mkdocs.yml` | not recoverable | [2026-08-23-adrian-changing-methodology.jsonl](transcripts/ai_transcripts/2026-08-23-adrian-changing-methodology.jsonl) |
| 2026-08-23 | Adrian | Qoder | Qoder | Documented client meeting notes from 2026-08-18 and 2026-08-21: added collapsible meeting entries to the client meetings page with attendees, agenda, decisions, actions (with due dates), and notes; captured feature priorities including custom API, database integration, code coverage, diagrams, project board, ML model targets, Swagger setup, and hosting topology pinger | `docs/meetings/client/index.md` | | [2026-08-23-adrian-qoder-client-meeting-docs.txt](transcripts/ai_transcripts/2026-08-23-adrian-qoder-client-meeting-docs.txt) |
| 2026-08-23 | Adrian | Qoder | Qoder | Final polish of all docs site pages: cross-referenced every doc against actual project codebase, rebuilt API endpoint table from controllers, rebuilt ERD from schema.prisma, updated architecture/tech-stack/feature-tiers/roadmap/wireframes/getting-started/security/ci-cd to reflect Sprint 1 completion, added CD section to CI/CD page, changed ADR-003 status to Accepted, fixed client meeting TBD dates and admonition syntax, added missing AI ledger entries | All docs pages | | |
| 2026-08-23 | Owen | Claude Code | Claude Sonnet 5 | Diagnosed CI failure (npm ci hanging on shared runner), hardened npm ci with fetch timeouts and retries in `.gitea/workflows/ci.yml`, committed and merged PR #46 (fix/cors-trusted-origins) into main | `.gitea/workflows/ci.yml` | | [2026-08-23-Owen-Claude-CI-fix-merge.txt](transcripts/ai_transcripts/2026-08-23-Owen-Claude-CI-fix-merge.txt) |
| 2026-08-23 | Kiran Soodyall | Claude | Claude Opus 5 | Landing page section reels (symbiote/crystalline themes), responsive background image sizing guidance, and 3-section landing page layout with image sections | `SectionReels.jsx`, `LandingPage.jsx` | | [kiran-2026-08-23.txt](transcripts/ai_transcripts/kiran-2026-08-23.txt) |
| 2026-08-23–24 | Adrian | Qoder | Qoder | Added 4 new transcripts (Owen sync, Owen CI fix, Kiran landing page, methodology change) with ledger entries; committed final docs polish (33 files); fixed client meeting attendees and sorted meetings by date; performed full Sprint 1 rubric audit against codebase; added team standup transcript and structured meeting summary to scrum meetings page; fixed broken admonition syntax in scrum meetings | `docs/ai-usage.md`, `docs/meetings/client/index.md`, `docs/meetings/scrum/index.md`, `docs/transcripts/ai_transcripts/`, `docs/transcripts/meeting-transcripts/` | not recoverable | [2026-08-23-adrian-qoder-docs-polish-session2.jsonl](transcripts/ai_transcripts/2026-08-23-adrian-qoder-docs-polish-session2.jsonl) | 
| 2026-08-24 | Daniel Passos | OpenAI Codex | GPT-5.6 Sol | Diagnosed the Google OAuth login failure, verified BetterAuth and Google callback configuration, identified invalid/missing OAuth credentials, tested successful sign-in/account creation/sign-out/account deletion, and unified the redesigned landing-page header across the rest of the application routes while preserving existing search, auth and route protection | `apps/web/src/**`, `apps/api/src/**`, `apps/web/src/components/AppLayout.tsx`, `apps/web/src/components/AuthStatus.tsx`, `apps/web/src/App.tsx` | | [Google OAuth and UI header work](transcripts/ai_transcripts/DanielPassos_03_GoogleOAuthAndUIHeader.txt) |
| 2026-08-24 | Kiran Soodyall | Claude | Claude Opus 5 | Diagnosed the Gitea `act_runner` repeatedly going offline and leaving Actions checks stuck pending (blocking merges, though Render/Cloudflare deploys run independently of it). Established that the runner is not a local machine and is most likely the shared SDP/Wits instance runner — evidenced by the "SHARED runner" wording at `ci.yml:35`, the `runs-on: default` label the workflow conforms to rather than chooses, artifact v4 being blocked on a Gitea upgrade the team cannot perform, and npm caching disabled rather than fixed. Compared independent runner options (SDP-provided shared runner, Oracle Cloud Always Free, Hetzner CX22, Azure for Students credit, mirror-to-GitHub) and recommended reporting the offline windows to SDP admins while standing up a team-owned runner with `restart: always`, an image-prune cron, and `runner.capacity: 1` plus swap to avoid OOM kills | No files changed — diagnosis only. Proposed but not yet written: ADR-004 (CI runner hosting), `docker-compose.runner.yml`, systemd unit, setup runbook |  | [kiran-2026-08-24.txt](transcripts/ai_transcripts/kiran-2026-08-24.txt) |
| 2026-08-24 | Josh Sawyer | Claude Code | Claude Sonnet 5 | Built a deployment diagram, full database ERD, backend class diagram, and a request-flow sequence diagram (all PlantUML, verified by rendering); corrected the deployment diagram after confirming the real DB host is Supabase (not local Postgres) and that Render/Cloudflare Pages hosting is genuinely live (ADR-003 status: Accepted); published all four diagrams to this docs site (Architecture Overview), switched them from PNG to SVG after the class diagram was found cut off at PlantUML's PNG raster cap, and added click-to-zoom via `mkdocs-glightbox`; removed the redundant ASCII "system shape" diagram; found and fixed a real gap in this ledger where two of Josh's existing transcripts (2026-08-06 scaffold, 2026-08-18 predictor/ingestion/Supabase work) were on disk but never linked from any row | `docs/design/architecture.md`, `docs/design/erd.md`, `docs/design/api-design.md`, `docs/design/diagrams/*.svg`, `mkdocs.yml`, `requirements.txt`, `docs/ai-usage.md` | | [JoshSawyer-conversation-2026-08-24-diagrams-and-docs-site.txt](transcripts/ai_transcripts/JoshSawyer-conversation-2026-08-24-diagrams-and-docs-site.txt) |
| 2026-08-24/25 | Sanele | Claude.ai | Claude Sonnet 5 | Diagnosed a Google OAuth `statemismatch` sign-in bug in apps/api's better-auth config — traced the actual resolved version (1.6.26, not the `^1.3.4` declared in package.json) and found the OAuth state cookie's 5-minute maxAge was shorter than its paired DB verification row's 10-minute lifetime, causing failures on slow sign-ins; also found the OAuth error redirect had no configured `errorURL` and was dead-ending on a raw Nest/Express 404 instead of the frontend. Fixed both (widened cookie maxAge to 10 min, added `onAPIError.errorURL`), and added self-service account deletion (enabled BetterAuth's `deleteUser`, added a 10-minute session `freshAge` re-auth guard) with new UI in AuthStatus.tsx and 5 new tests; verified with a full typecheck + test suite run (9/9 new, 87/87 total). Separately diagnosed a follow-up local-testing failure to missing Google OAuth credentials in the local `.env` (not a regression), walked through reusing the team's existing Render-hosted Google OAuth client, and resolved a redirect-URI mismatch. Verified deletion end-to-end (cascade deletes, cookie clearing, re-signup, freshness guard) via direct Postgres queries. Did a final cleanup pass (fixed a doc-path typo, confirmed no leftover debug code, reran lint/tests), wrote a PR-ready summary doc, and updated `package.json`'s better-auth version to match the resolved 1.6.26 | `apps/api/src/auth/auth.config.ts`, `apps/web/src/components/AuthStatus.tsx`, `apps/web/src/components/AuthStatus.spec.tsx`, `docs/bugs/auth-state-mismatch.md`, `package.json` | | [claude-ai-google-auth-bug.txt](transcripts/ai_transcripts/claude-ai-google-auth-bug.txt) |
| 2026-08-22 | Sanele | Claude.ai | Claude Sonnet 5 | Researched what a Render "pinger" is and why the sportsanalytics API needs one (Render free tier spins down after ~15 min idle); compared external cron-ping services vs. GitHub/Gitea Actions cron workflows and recommended the external service as more reliable given self-hosted Gitea's extra failure points; confirmed the existing `/health` endpoint on apps/api (no global prefix, no DB dependency) working both locally and on the deployed Render URL; set up cron-job.org to ping it every 10 minutes with response-saving enabled; flagged and resolved via web search a real risk around Render's 750 free instance-hour/month cap — running the pinger 24/7 uses nearly the full monthly allowance, so it's only safe while the API is the only always-on free service on the team's Render workspace; agreed to revisit the schedule if another always-on service gets added. No code changes made — the `/health` route already existed; the only new artifact is the cron-job.org config, which lives outside the repo | None (existing `/health` route reused, not modified; cron-job.org configuration only) | | [claude-ai-share-render-pinger.txt](transcripts/ai_transcripts/claude-ai-share-render-pinger.txt) |
| 2026-08-21 | Sanele | Claude.ai | Claude Sonnet 5 | Explored and documented the `CommonPlayerInfo` stats.nba.com endpoint (and related bulk endpoints) to plan a player-profiles feature; identified that `CommonPlayerInfo`'s team fields only reflect a player's *current* team with no season parameter, so they go stale after mid-season trades, and recommended pulling season-aware team data from `CommonTeamRoster` or from already-ingested play-by-play events instead. Implemented the player bio feature: added 9 nullable bio columns to the `Player` Prisma schema + migration, wrote a new `player_bios.py` ingestion module (handles the ISO-timestamp birthdate and "Undrafted"-string edge cases, never touches team fields), wired it into `ingest.py`, added the fields to the `Player` TS interface, and added a "Bio" card to `PlayerProfilePage.tsx`. Debugged a local `ModuleNotFoundError` for `nba_api` and verified the pipeline end-to-end against local Postgres. Wrote and ran a standalone `backfill_player_bios.py` to enrich bios for the ~12 players already in the dev seed data. In the process, discovered a pre-existing bug via an automatic name cross-check: two `seed.ts` `MOCK_PLAYERS` entries were linked to the wrong real NBA player IDs (Derrick White ↔ Coby White, Buddy Hield ↔ Kevon Looney); wrote a standalone bug report recommending the correct IDs and flagging that already-seeded databases won't self-correct from a code fix alone. Updated `player-bio-feature.md` and `ingestion/README.md` to reflect the blocked status, cross-link the new bug doc, and document the cross-check logic | `prisma/schema.prisma`, `prisma/migrations/20260819120000_player_bio_fields/`, `apps/ingestion/player_bios.py`, `apps/ingestion/ingest.py`, `apps/ingestion/backfill_player_bios.py`, `apps/ingestion/generate_seed_bios.py`, `types/nba.ts`, `PlayerProfilePage.tsx`, `docs/player-bio-feature.md`, `docs/seed-nbaplayerid-bug.md`, `apps/ingestion/README.md` | | [ai-usage-transcript-player-bios.txt](transcripts/ai_transcripts/ai-usage-transcript-player-bios.txt) |
| 2026-09-11 | Owen | Claude Code | Claude Sonnet 5 | Reviewed PRs #86 (swagger-openapi), #89 (postseason-view), #90 (advanced-player-stats) against main via test-merges in a throwaway worktree (typecheck + full web test suite, 188/188 passing); found and resolved a real merge conflict in `players.controller.ts` between the Swagger decorators (#86) and the `seasonType` query-param work already merged from #89/#90, then merged #86 into main directly (Gitea had no merge button since main had advanced past it); audited the docs site against the current Sprint 2 rubric and found `docs/api-reference.md` and `docs/rubric-links.md` claiming Swagger was already live when it wasn't yet merged; backfilled three weeks of `sprint-log.md` (25 Aug – 11 Sep) from git history and the scrum meeting notes | `apps/api/src/players/players.controller.ts` (merge conflict resolution), `docs/sprint-log.md`, `docs/ai-usage.md` | | |
| 2026-09-11 | Owen | Claude Code | Claude Sonnet 5 | Full accuracy sweep of every remaining docs-site page against source (controllers, migrations, the real `.gitea/workflows/ci.yml`, components): rewrote the CI/CD Postgres section after finding it still described a `services:` container on `runs-on: default`, when commits from earlier that day had already switched to `runs-on: ubuntu-latest` and a hand-rolled disposable-Postgres step (three `services:` attempts never worked on this runner's `network: host` setup); fixed the same stale "axe-core planned" claim in three more pages; corrected `demo-guide.md`'s description of the landing/home split and rewrote its walkthrough to include Compare and the season-segment control; caught a discrepancy between the team's own meeting notes (claiming the home page was "wired to real data") and the component's own code comment ("NOT WIRED TO THE API YET") and went with the code. Then reviewed PR #88 (LandingPageUpdates): found `ci-runner/data/.runner`, a live Gitea Actions runner registration token (`kiran-backup`, self-hosted, label `default`) committed to the repo by accident; test-merged and confirmed no real code bug (189/189 tests, clean typecheck/lint — the CI failures shown were shared-runner flakiness), resolved a real conflict in `LandingPage.spec.tsx` against #91's changes, stripped the runner secret, and merged #88 into main without it. Diagnosed a recurring "sign-in link expired" report: BetterAuth's OAuth state row hard-expires 10 minutes after sign-in starts with no config option in the library (confirmed by downloading and reading `better-auth@1.7.4`'s source, the latest release) — added a fire-and-forget `/health` ping on `AuthStatus` mount to pre-warm Render's free-tier instance before the user reaches the sign-in button, guarded out of test mode since the component renders in nearly every page's test suite | `apps/web/src/pages/LandingPage.spec.tsx` (merge conflict resolution), `apps/web/src/components/AuthStatus.tsx`, `apps/web/src/lib/apiClient.ts`, `docs/ci-cd.md`, `docs/index.md`, `docs/demo-guide.md`, `docs/requirements.md`, `docs/sprint-log.md`, `docs/ai-usage.md` | | |
| 2026-09-10 | Daniel Passos | OpenAI Codex | GPT-5.6 Sol | Reviewed teammate PRs #86 and #89, audited quick Sprint 2 backlog options, and implemented automated axe-core accessibility checks for Home, Players, Predictions and Optimizer, including accessible names for the Players filters | apps/web/package.json, apps/web/package-lock.json, apps/web/src/test/accessibility.ts, apps/web/src/pages/HomePage.spec.tsx, apps/web/src/pages/PlayersListPage.spec.tsx, apps/web/src/pages/PredictionsPage.spec.tsx, apps/web/src/pages/OptimizerPage.spec.tsx, apps/web/src/components/PlayersFilterBar.tsx, apps/web/src/components/PlayersFilterBar.spec.tsx | | [PR reviews and accessibility work](transcripts/ai_transcripts/DanielPassos_04_2026-09-10_AccessibilityAndPRReviews.txt) |
| 2026-09-13 | Daniel Passos | OpenAI Codex | GPT-6 Astra | Reviewed the current Sprint 2 backlog, assessed CI hardening and secret-scanning requirements, and implemented personalization fixes for follow/unfollow persistence, failed writes, pending saves and locker cache invalidation using the existing /v1/me architecture | apps/web/src/components/FollowPlayerButton.tsx, apps/web/src/components/FollowPlayerButton.spec.tsx, apps/web/src/components/FollowTeamButton.tsx, apps/web/src/components/FollowTeamButton.spec.tsx, apps/web/src/pages/OnboardingPage.tsx, apps/web/src/pages/OnboardingPage.spec.tsx, apps/web/src/pages/ProfilePage.tsx, apps/web/src/pages/ProfilePage.spec.tsx, apps/web/src/lib/preferenceQueries.ts, docs/PROJECT_OVERVIEW.md | | [Personalization and backlog work](transcripts/ai_transcripts/DanielPassos_05_2026-09-13_PersonalizationAndBacklog.txt) |
| 2026-09-12 | Owen | Claude Code | Claude Sonnet 5 | Diagnosed a live production sign-in bug affecting only Safari/Firefox/Brave users: the OAuth CSRF state cookie was set via a cross-origin `fetch()`, which those browsers' stricter third-party cookie policies silently dropped before Google's callback could read it back, producing an inconsistent `state_mismatch`. Removed the secondary state-cookie check (PR #100), then found sign-in still failed afterward because the session cookie itself was genuinely cross-site (API on Render, SPA on Cloudflare Pages) — fixed by proxying `/api` and `/auth` through same-origin Cloudflare Pages Functions so every cookie is first-party (PR #108), relocating those Functions to the repo root after confirming Cloudflare's configured root directory never found them under `apps/web/functions/` (PR #112), and clearing a stale `VITE_API_BASE_URL` Cloudflare env var that was silently bypassing the whole proxy by baking a direct cross-origin API URL into the build (PR #117, confirmed via the deployed bundle and a browser console CORS error). Verified end-to-end against the live production site at every step, not just locally. Also fixed the OAuth production-origin fallback (PR #104), switched CI to the course-provided `sdp-runner-1` runner (PR #107), added a migrate-deploy retry for intermittent CI failures (PR #116), and added a coverage test to clear the enforced 80% threshold (PR #113) | `apps/api/src/auth/auth.config.ts`, `functions/api/[[path]].ts`, `functions/auth/[[path]].ts`, `apps/web/src/lib/apiBase.ts`, `apps/web/src/lib/apiClient.ts`, `apps/web/src/lib/authClient.ts`, `apps/web/vite.config.ts`, `apps/api/test/global-setup.ts`, `.gitea/workflows/ci.yml`, `apps/web/src/lib/meApi.spec.ts` | | |
| 2026-09-13 | Owen | Claude Code | Claude Sonnet 5 | Restyled the Compare and Teams pages — the last two pages still on the original dark app-shell theme after every other page had already moved to the lighter "locker" design language (PlayerSearchCombobox/Pagination's own code comments called out these two by name as the holdouts). Swapped `SeasonSegmentControl` for `LockerSegmentControl`, recoloured every card/tile/button/stat cell to the locker palette, no behavioural changes (PR #118) | `apps/web/src/pages/ComparePage.tsx`, `apps/web/src/pages/TeamsListPage.tsx`, `apps/web/src/components/PlayerSearchCombobox.tsx`, `apps/web/src/components/Pagination.tsx` | | |
| 2026-09-13 | Owen | Claude Code | Claude Sonnet 5 | Built an admin page wiring up the RBAC infrastructure that already existed in the schema (`Role` enum, `RolesGuard`, `@Roles()`) but had no endpoint using it: new `/v1/admin/teams`, `/v1/admin/players`, `/v1/admin/users` routes gated by `SessionAuthGuard` + `@Roles(ADMIN)` — the API's first write access to `Team`/`Player` rows — with a Teams/Players/Users tabbed admin UI, self-protection against deleting/demoting your own account, and one manual DB promotion for the first admin account. Verified with service-level unit tests (no local Postgres available) and safe, credential-free Playwright screenshots using network-level route mocking rather than a real or fabricated session. Pushed to its own branch, not yet merged, for a teammate to continue (PR #120, open) | `apps/api/src/admin/**`, `apps/api/src/app.module.ts`, `apps/api/src/me/me.service.ts`, `apps/web/src/pages/AdminPage.tsx`, `apps/web/src/components/AdminGate.tsx`, `apps/web/src/lib/adminApi.ts`, `apps/web/src/App.tsx` | | |
| 2026-09-13 | Owen | Claude Code | Claude Sonnet 5 | Read through a pasted rubric critique of the predictions feature (5 gaps: no model versioning, predictions not derived from raw event data, no real API-level correctness test, validation methodology not checked into the repo, no async job pattern for regenerating predictions) against the actual `apps/predictor` and `GamePrediction` code before changing anything — found the bundled marking-rubric assets in this session's `sdp` skill actually describe a different, generic project, so treated the pasted critique as authoritative instead. Scoped the "derive from GameEvent" gap honestly: `apps/ingestion` only ever writes two placeholder bookend rows per game into `GameEvent`, so genuinely deriving predictions from raw play-by-play would mean a new ingestion feature (pulling `PlayByPlayV3`), not a predictor fix — deferred rather than faked, given the Sprint 2 deadline. Shipped step 1: added a `modelVersion` column to `GamePrediction` plus a new append-only `GamePredictionRun` history table (upserted per game+version, never overwritten across a version change), a `MODEL_VERSION` constant in `predict_games.py`, and `GET /v1/games/:id/prediction/history` — with zero changes to any of the 7+ existing places that read the `game.prediction` relation (PR #126, open, first of a multi-step series) | `apps/api/prisma/schema.prisma`, `apps/api/prisma/migrations/20260913140000_game_prediction_versioning/`, `apps/predictor/predict_games.py`, `apps/predictor/test_predict_games.py`, `apps/api/src/games/games.service.ts`, `apps/api/src/games/games.controller.ts`, `apps/api/test/games.e2e-spec.ts` | | |
| 2026-09-13 | Owen | Claude Code | Claude Sonnet 5 | Updated the docs site's Bug Tracker evidence: rewrote `testing.md`'s "Bug tracking" section, which had described a severity scheme (critical/major/minor/cosmetic) that was never real — the actual system is `.gitea/ISSUE_TEMPLATE/bug_report.yaml`'s structured form plus `priority/`+component labels, documented in the main repo's `docs/BUG_TRACKING.md`. Embedded a real screenshot of Gitea's "New Issue" template picker (supplied by Owen) and cited a real filed-and-closed example (issue #83) fetched via the Gitea API. Updated `rubric-links.md`'s Bug Tracker row to point at this instead of only the Gitea Projects Board, which requires authorisation a marker won't have. Left the User Feedback evidence untouched — no live survey or Gitea issue content exists yet to screenshot honestly | `docs/testing.md`, `docs/rubric-links.md`, `docs/assets/screenshots/gitea-new-issue-templates.png` | | |
| 2026-08-29 | Sanele | Claude Code | Claude Opus 5 | Updated the unmerged `player-bio-fields` branch with the latest main, restyled the profile page's Bio section to match the rest of the page, and replaced the misleading "Undrafted" shown for players whose bios hadn't been ingested yet with a loading state. Found that the earlier bio PR was reverted only because its migration hadn't been run, so added `prisma migrate deploy` to the API start command on Render; drafted the re-land PR (#52) | `apps/web/src/pages/PlayerProfilePage.tsx`, `apps/web/src/pages/PlayerProfilePage.spec.tsx`, `render.yaml` | | [sanele-player_bio_updates_implementation.txt](transcripts/ai_transcripts/sanele-player_bio_updates_implementation.txt) |
| 2026-08-29–30 | Sanele | Claude Code | Claude Opus 5 | Checked which stats the database already held for a player comparison feature, then built it: per-game season averages and a `GET /v1/players/compare` endpoint, a Compare page (up to 4 players side by side, grouped table-style rows, leader highlighting, player search combobox) iterated on from screenshots, and a compare link plus minutes/free-throw attempts on the profile page. Planned a local test run against the prod database so migrations are confirmed before merging; drafted the PR (#53) | `apps/api/src/players/stats.service.ts`, `apps/api/src/players/players.controller.ts`, `apps/api/test/players.e2e-spec.ts`, `apps/web/src/pages/ComparePage.tsx`, `apps/web/src/components/PlayerSearchCombobox.tsx`, `apps/web/src/lib/nbaApi.ts`, `apps/web/src/types/nba.ts`, `apps/web/src/App.tsx`, `apps/web/src/components/landing/LandingHeader.tsx`, `apps/web/src/pages/PlayerProfilePage.tsx`, `docs/PROJECT_OVERVIEW.md` | | [sanele-player_comparisons_implementation.txt](transcripts/ai_transcripts/sanele-player_comparisons_implementation.txt) |
| 2026-09-04–06 | Sanele | Claude Code | Claude Opus 5 | Diagnosed why PR #69 (player test mock fixes) conflicted with main: the same fixture fixes had already landed through #81, so the PR was redundant and could be closed. On a new `compare-page-age` branch, fixed age not showing on the Compare page (the data was already returned but the row was a hardcoded dash) by moving `calculateAge` and the bio formatting helpers into a shared `playerBio.ts` used by both pages, with tests; wrote the PR description | `apps/web/src/lib/playerBio.ts`, `apps/web/src/lib/playerBio.spec.ts`, `apps/web/src/pages/ComparePage.tsx`, `apps/web/src/pages/ComparePage.spec.tsx`, `apps/web/src/pages/PlayerProfilePage.tsx` | | [sanele-fixing_prs_with_conflicting_files.txt](transcripts/ai_transcripts/sanele-fixing_prs_with_conflicting_files.txt) |
| 2026-09-07–11 | Sanele | Claude Code | Claude Opus 5 | Implemented the postseason view (#89): season type and playoff round on games, ingestion of play-in/playoff/finals games, a `seasonType` filter on player and game endpoints, a season segment control and regular-season vs postseason splits in the web app, and kept postseason games out of the predictor and optimizer models. Then added advanced stats (#90): stored +/-, usage and player ratings per game, derived TS% and eFG%, and showed them on the profile, splits and compare pages, with a rate-limit-safe backfill script run against prod after taking a database backup. Fixed a reviewer-reported bug where the selected segment was lost when following profile and compare links | `apps/api/prisma/schema.prisma`, `apps/api/prisma/migrations/20260907101500_add_game_season_type/`, `apps/api/prisma/migrations/20260907170000_add_advanced_player_game_stats/`, `apps/ingestion/games.py`, `apps/ingestion/ingest.py`, `apps/ingestion/ingest_postseason.py`, `apps/ingestion/player_game_logs.py`, `apps/ingestion/backfill_advanced_stats.py`, `apps/api/src/common/season-type.ts`, `apps/api/src/players/stats.service.ts`, `apps/api/src/players/players.service.ts`, `apps/api/src/games/games.service.ts`, `apps/predictor/elo.py`, `apps/predictor/four_factors.py`, `apps/optimizer/predict.py`, `apps/web/src/lib/seasonType.ts`, `apps/web/src/lib/advancedStats.ts`, `apps/web/src/components/SeasonSegmentControl.tsx`, `apps/web/src/components/SeasonSplitsTable.tsx`, `apps/web/src/pages/PlayerProfilePage.tsx`, `apps/web/src/pages/PlayersListPage.tsx`, `apps/web/src/pages/ComparePage.tsx` | | [sanele-postseason-view_implementation.txt](transcripts/ai_transcripts/sanele-postseason-view_implementation.txt) |
| 2026-09-13 | Kiran Soodyall | Claude Code | Not recorded in the export | Audited every database read the API makes and cut the volume down (PR #124). Found that nothing was cached anywhere: React Query's defaults re-ran all 41 `useQuery` calls on mount and on every tab switch, every signed-in request re-read the `Session` and `User` tables through BetterAuth, `/players/leaders` and sorted player lists scanned the whole player table plus a `groupBy` over ~80k boxscore rows, both analytics routes scanned every completed game, and `Game` had no index on `gameDate`, `homeTeamId` or `awayTeamId`. Built an in-process response cache (single-flight so concurrent identical loads share one query, never caching errors or nulls so a 404 can't stick, bounded at ~500 entries, disabled under Vitest so e2e specs can't read a previous spec's rows), applied at the service layer rather than as a controller interceptor so shared sub-results are cached once; consolidated five endpoints' redundant queries (splits 5→2, compare 8→2 for four players, stats 3→2, prediction 2→1, leaderboard counts 3→2) and deleted `PredictionsService` once it had no callers left; set a 5-minute React Query `staleTime` with no refetch on window focus; enabled BetterAuth's session cookie cache; and added indexes on `Game`, `PlayerGameStat` and `Player`. Measured with `PRISMA_LOG_QUERIES=true` against a local database holding real data: most public endpoints drop from 1–6 statements to 0 on a repeat call. Separately fixed Beat the Model (the card kept showing the game just called while the next loaded, replayed a called game after navigating back, and stuck on a 409) and a leaderboard bug where two same-named users could swap rows; verified five consecutive calls returned five different real games inside a rolled-back transaction. Also diagnosed a 502-on-every-request incident mid-session and traced it to `apps/api/.env` having been overwritten with a copy of `.env.example`, which crashed the API at boot on an invalid Supabase URL — unrelated to the caching work | `apps/api/src/cache/**`, `apps/api/src/prisma/prisma.service.ts`, `apps/api/src/games/**`, `apps/api/src/players/**`, `apps/api/src/teams/**`, `apps/api/src/analytics/**`, `apps/api/src/me/picks/**`, `apps/api/src/auth/auth.config.ts`, `apps/api/prisma/schema.prisma`, `apps/web/src/main.tsx`, `apps/web/src/components/home/BeatTheModelCard.tsx`, `apps/web/src/components/home/LeaderboardCard.tsx`, `apps/api/.env.example` | | Exported to the app repo's AI Chat Docs folder; not yet copied into this site's `transcripts/ai_transcripts/` |
| 2026-09-16 | Daniel Passos | OpenAI Codex | Not specified | Reviewed remaining requirements, selected quick independent tasks, added public API contract testing for requirement #37, and prepared the API deprecation-path implementation for requirement #36 | `apps/api/test/openapi-contract.e2e-spec.ts`, `apps/api/vitest.config.ts`, `apps/api/src/common/deprecated-endpoint.decorator.ts`, `apps/api/src/common/deprecation.interceptor.spec.ts`, `apps/api/src/app.module.ts` | | [Requirements and API contract work](transcripts/ai_transcripts/DanielPassos_06_2026-09-16_RequirementsAndAPIContracts.txt) |
| 2026-09-19 | Sanele | Claude Code | Claude Opus 5 | Built the admin event-corrections workflow (PR #182): game lookup + resolved-credit play-by-play admin endpoints, a correction preview/apply/undo flow with validation (rejects a correction that leaves a play's credit on the wrong player, requires a reason), a `plan-stat-recompute`/`game-snapshot` path so only the affected player's stats are recomputed, and the admin UI (game picker, play-by-play panel, edit form, correction history) | `apps/api/src/admin/admin-events.*`, `apps/api/src/admin/admin-games.*`, `apps/api/src/admin/{event-correction-request,event-correction-rules,plan-stat-recompute,game-snapshot,derive-player-game-stats}.ts`, `apps/api/prisma/schema.prisma`, `apps/web/src/components/admin/**`, `apps/web/src/lib/adminApi.ts`, `apps/web/src/pages/AdminPage.tsx` | | |
| 2026-09-19 | Sanele | Claude Code | Claude Opus 5 | Fixed teammate-credit resolution in the corrections workflow above: disambiguated teammates sharing a surname and initial, matched the prefix NBA's own feed writes for a player name, and kept the correction vocabulary in step with ingestion's own translation (PR #183, merged after #182) | `apps/api/src/admin/{derive-player-game-stats,event-correction-rules}.ts`, `apps/ingestion/{derive_player_game_stats.py,ingest.py,ingest_historical_season.py,rosters.py}` | | |
| 2026-09-23 | Owen | Claude Code | Claude Sonnet 5 | Marked the whole project against the COMS3011A rubric (event-sourcing, submission review, dataset releases, API maturity, testing, performance, documentation currency) — found and fixed two real bugs (review status didn't gate public reads; a batch-resume mechanism could be silently defeated by a crash mid-phase), added a load-test script against a stated performance target, filled a missing `AdminGamesService` unit spec, backfilled the main repo's `ai-usage.md`/`PROJECT_OVERVIEW.md`, and pushed 6 feature branches for review. Safely reverted 2 branches accidentally merged straight to `main` (new commits, not history rewriting), then recreated them as fresh branches once Gitea's "Merged" badge turned out to be permanent by design. Then did a full accuracy sweep of this documentation site: rewrote `design/feature-tiers.md` against the brief's actual tier language (the previous version described a different, ML-first product framing that predated the real event-sourcing work), and fixed real factual errors across ~20 pages — a fabricated credential/email sign-in path, "rate limiting not implemented" when it's real, "nothing in the schema supports submission review" when it's fully built, a false "password reset delivered" claim contradicting the same document's own later row, a fabricated "password change" test, and a getting-started.md step missing entirely (mandatory API keys) that would have blocked a new contributor. Also fixed the sprint log's Week of 15 Sep table (rebuilt with real per-PR git authorship instead of a nonstandard PR-number column) and stripped AI-tool attribution from 15 pre-existing Owner-column rows, per the team's own convention that this ledger — not the sprint log — is where AI tool/model is recorded | `apps/api/src/common/game-visibility.{ts,spec.ts}`, `apps/api/src/players/players.service.{ts,spec.ts}`, `apps/api/src/{games,datasets,custom-statistics,me/follows}/**`, `apps/api/test/game-review-gating.e2e-spec.ts`, `apps/api/src/admin/admin-games.service.spec.ts`, `apps/ingestion/{ingest.py,play_by_play.py,test_play_by_play.py}`, `apps/api/scripts/load-test.mjs`, `docs/PROJECT_OVERVIEW.md`, `docs/ai-usage.md`, `docs/decisions/ADR-004-oauth-only-authentication.md` (main repo); `docs/index.md`, `docs/sprint-log.md`, `docs/ai-usage.md`, `docs/design/{feature-tiers,roadmap,api-design,wireframes,erd,architecture}.md`, `docs/api-reference.md`, `docs/{security,testing,tech-stack,rubric-links,getting-started,ci-cd,stakeholder-interactions}.md`, `docs/decisions/{adr-001-database,adr-003-hosting-topology}.md` (this site) | not recoverable | [Owen Claude SDP Marking And Fixes](transcripts/ai_transcripts/2026-09-23-Owen-Claude-SDP-Marking-And-Fixes.txt)
